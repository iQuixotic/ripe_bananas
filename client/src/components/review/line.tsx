import * as React from 'react';

/**
 * just a line guys. Nothing to see here.
 * @param props notihing passed in
 */
const Line: React.FunctionComponent<any> = (props) => {
  return(
    <div className="col-12 page-br"></div>
  );
};

export default Line;
